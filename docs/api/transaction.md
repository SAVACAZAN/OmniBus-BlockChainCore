# Module: `transaction`

> Transaction structure and validation — defines TX fields (from, to, amount, fee, nonce, signature), signing via secp256k1, hash integrity checks, and fee calculations.

**Source:** `core/transaction.zig` | **Lines:** 388 | **Functions:** 5 | **Structs:** 1 | **Tests:** 14

---

## Contents

### Structs
- [`Transaction`](#transaction) — A single transaction transferring value between addresses — includes sender, rec...

### Constants
- [1 constants defined](#constants)

### Functions
- [`calculateHash()`](#calculatehash) — Calculeaza hash-ul tranzactiei (SHA256d — Bitcoin style)
Hash = SHA256...
- [`isValid()`](#isvalid) — Valideaza tranzactia: amount > 0 (or op_return TX), adrese cu prefix c...
- [`sign()`](#sign) — Semneaza tranzactia cu private key (secp256k1 ECDSA SHA256d — REAL)
Se...
- [`verify()`](#verify) — Verifica semnatura tranzactiei cu public key (secp256k1 ECDSA — REAL)
- [`verifyWithHexPubkey()`](#verifywithhexpubkey) — Verifica semnatura cu public key in format hex (66 chars)

---

## Structs

### `Transaction`

A single transaction transferring value between addresses — includes sender, recipient, amount, fee, and cryptographic signature.

| Field | Type | Description |
|-------|------|-------------|
| `id` | `u32` | Id |
| `from_address` | `[]const u8` | From_address |
| `to_address` | `[]const u8` | To_address |
| `amount` | `u64` | Amount |
| `fee` | `u64` | Fee |
| `timestamp` | `i64` | Timestamp |
| `nonce` | `u64` | Nonce |
| `op_return` | `[]const u8` | Op_return |
| `locktime` | `u64` | Locktime |
| `script_pubkey` | `[]const u8` | Script_pubkey |
| `script_sig` | `[]const u8` | Script_sig |
| `signature` | `[]const u8` | Signature |
| `hash` | `[]const u8` | Hash |

*Defined at line 9*

---

## Constants

| Name | Value | Description |
|------|-------|-------------|
| `MAX_OP_RETURN` | `usize = 80` | M a x_ o p_ r e t u r n |

---

## Functions

### `calculateHash()`

Calculeaza hash-ul tranzactiei (SHA256d — Bitcoin style)
Hash = SHA256(SHA256(id || from || to || amount || timestamp || nonce))
Nonce inclus in hash previne replay attacks (aceeasi TX cu nonce diferit = hash diferit)

```zig
pub fn calculateHash(self: *const Transaction) [32]u8 {
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `self` | `*const Transaction` | The instance |

**Returns:** `[32]u8`

*Defined at line 56*

---

### `isValid()`

Valideaza tranzactia: amount > 0 (or op_return TX), adrese cu prefix corect, op_return <= 80 bytes

```zig
pub fn isValid(self: *const Transaction) bool {
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `self` | `*const Transaction` | The instance |

**Returns:** `bool`

*Defined at line 111*

---

### `sign()`

Semneaza tranzactia cu private key (secp256k1 ECDSA SHA256d — REAL)
Seteaza self.signature = hex(R||S) si self.hash = hex(tx_hash)

```zig
pub fn sign(self: *Transaction, private_key: [32]u8, allocator: std.mem.Allocator) !void {
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `self` | `*Transaction` | The instance |
| `private_key` | `[32]u8` | Private_key |
| `allocator` | `std.mem.Allocator` | Allocator |

**Returns:** `!void`

*Defined at line 134*

---

### `verify()`

Verifica semnatura tranzactiei cu public key (secp256k1 ECDSA — REAL)

```zig
pub fn verify(self: *const Transaction, compressed_pubkey: [33]u8) bool {
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `self` | `*const Transaction` | The instance |
| `compressed_pubkey` | `[33]u8` | Compressed_pubkey |

**Returns:** `bool`

*Defined at line 147*

---

### `verifyWithHexPubkey()`

Verifica semnatura cu public key in format hex (66 chars)

```zig
pub fn verifyWithHexPubkey(self: *const Transaction, pubkey_hex: []const u8) bool {
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `self` | `*const Transaction` | The instance |
| `pubkey_hex` | `[]const u8` | Pubkey_hex |

**Returns:** `bool`

*Defined at line 167*

---


---

*Generated by OmniBus Doc Generator v2.0 — 2026-03-31 02:16*
