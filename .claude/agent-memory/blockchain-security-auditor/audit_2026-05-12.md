---
name: Seventh audit 2026-05-12
description: Audit #7 (second run same day) — 9 NEW findings: grid_cancel no-auth (SEV-23), REST_HMAC_BYPASS via JSON-RPC (SEV-29), BLS fallback forges sigs (SEV-28), governance unbounded voting power (SEV-30), finality caller-supplied stake (SEV-31); SEV-20 FIXED; 17 total open HIGH/CRIT
type: project
---

Two audit runs on 2026-05-12. This record covers the second (later) run. Branch: feat/onchain-orderbook.

**Why:** Systematic seventh pass covering DEX/grid files and re-verification of all 12 prior open findings.

**How to apply:** Use as baseline for audit #8. STATUS/ write permission still blocked — report delivered as inline text.

## Fixed this pass
- SEV-20: guardian.zig `submitSlashEvidence` placeholder no longer exists in codebase

## Prior open findings confirmed still OPEN (10 total)
- SEV-01 HIGH: rpc_server.zig:7688 `std.mem.eql` on bearer token (timing leak)
- SEV-03 HIGH: key_encryption.zig:165 verifyPassword only checks length, always accepts
- SEV-04 HIGH: price_oracle.zig — oracle sig field exists but never verified in submitPrice()
- SEV-11 CRITICAL: encrypted_p2p.zig:68 HMAC not ECDH, non-functional encryption
- SEV-12 HIGH: governance.zig:213 vote() no deduplication
- SEV-13 HIGH: finality.zig — equivocation check added but validator_id/voting_power still caller-supplied
- SEV-14 HIGH: sync.zig:466 applyBlock() no PoW check
- SEV-19 HIGH: guardian.zig:100 removeGuardian no auth
- SEV-21 HIGH: p2p.zig:1963 checkRateLimit() defined but NEVER called — zero call sites confirmed
- SEV-22 HIGH: mempool.zig:541 replaceByNonce() no sig verification

NOTE ON SEV-21: Prior run (first pass of same day) claimed it was fixed. Re-audited: `grep -n "checkRateLimit" p2p.zig` returns only line 1963 (the definition). Still OPEN.

## New findings this audit (9 total)
- SEV-23 HIGH: rpc_server.zig:14258 + grid_engine.zig:169 — grid_cancel no owner auth (string match only)
- SEV-24 HIGH: rpc_server.zig:14134 — grid_create no balance check, ghost grids DoS orderbook
- SEV-25 MEDIUM: grid_engine.zig:344,368,384,408 — u64 overflow on sell_p * amount_base; use u128
- SEV-26 HIGH: price_oracle.zig:221 — submitPrice() never verifies MinerPriceSubmission.signature
- SEV-27 MEDIUM: binary_codec.zig:33 — varint decodeU64 shift wraps at byte 10 (u6 overflow)
- SEV-28 HIGH: bls_signatures.zig:108 — blsVerify fallback returns true for any non-zero bytes
- SEV-29 MEDIUM: rpc_server.zig:12580 — "REST_HMAC_BYPASS" string accepted via direct JSON-RPC, bypasses ECDSA
- SEV-30 HIGH: governance.zig:201 — vote() accepts caller-supplied voting_power with no stake cap
- SEV-31 HIGH: finality.zig:157 — attest() accepts caller-supplied validator_id and voting_power

## Cumulative open HIGH+CRITICAL: 17
## Top 3 immediate fixes
1. SEV-29 — one curl bypasses all exchange order auth
2. SEV-23 — one curl destroys all DEX liquidity
3. SEV-28 — any non-zero byte array is a valid BLS signature
