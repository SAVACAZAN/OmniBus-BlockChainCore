---
name: sixteenth-audit-2026-05-25
description: Sixteenth security audit session — 4 NEW HIGH/MEDIUM in vault/covenant/treasury handlers, SEV-70 FIXED, 2 CRIT + 9 HIGH still open
metadata:
  type: project
---

Sixteenth audit session (2026-05-25). Files audited: secp256k1.zig, rpc_server.zig, p2p.zig, mempool.zig, transaction.zig, state_trie.zig, governance.zig, governance_onchain.zig, grid_engine.zig, matching_engine.zig, block.zig, key_encryption.zig, covenant.zig, timelock_vault.zig (new), treasury_multi.zig (new).

**FIXED:**
- SEV-70: governance_onchain.zig uses `+|=` (saturating add) for yes_weight/no_weight — overflow fixed.
- SEV-66: already confirmed fixed (grid_cancel owner check).

**NEW findings:**
- SEV-85 HIGH: rpc_server.zig:18020 — `timelock_spend` no ownership check; anyone can trigger spend by vault_id.
- SEV-86 HIGH: rpc_server.zig:18167 — `covenant_remove` no ownership check; anyone can remove any address's covenant.
- SEV-87 HIGH: rpc_server.zig:18271 — `treasury_distribute` no auth; anyone can trigger treasury distribution.
- SEV-88 MEDIUM: rpc_server.zig:17950 — `timelock_create` no owner signature; anyone can lock another user's exchange balance.
- SEV-89 MEDIUM: grid_engine.zig:205,224 — grid order_id namespace collision with manual orders (grid_id*10000 scheme).
- SEV-90 MEDIUM: rpc_server.zig:7819 — precise re-confirmation: `std.mem.eql` used for bearer token; constantTimeEql at line 7790 never called from isAuthorized.

**STILL-OPEN confirmed:**
- SEV-42 CRITICAL: transaction.zig:641-642 — pq_omni/hybrid schemes return `true` unconditionally.
- SEV-50 CRITICAL: state_trie.zig:93 — AutoHashMap iteration non-deterministic, state root non-deterministic.
- SEV-01/72/79/90 HIGH: rpc_server.zig:7819 — std.mem.eql for bearer token.
- SEV-04/71 HIGH: key_encryption.zig:165 — verifyPassword is length-check stub.
- SEV-43/60 HIGH: block.zig:159 — fills_root absent from calculateHash.
- SEV-58 HIGH: rpc_server.zig:16189 — gov_vote tier self-declared by caller, no on-chain lookup.
- SEV-75 HIGH: rpc_server.zig multiple — ctx.bc.mempool.append bypasses Mempool.add at 7+ sites.
- SEV-78 HIGH: p2p.zig:2445 — dispatchMessage never calls checkRateLimit.
- SEV-83 HIGH: mempool.zig:541 — replaceByNonce skips verifier/fee/size checks.
- SEV-84 MEDIUM: transaction.zig — no chain_id in hash preimage.

**Why:** Three new vault/covenant/treasury modules added without consistent ownership auth pattern. New pattern needed: shared `requireOwnerSig` helper for all mutation handlers.

**How to apply:** In future sessions, when scanning new RPC handlers, check if mutation handlers require signature verification against the named address. Vault-style features (timelock, covenant, treasury) are a recurring pattern in this codebase and tend to miss auth.

Status/SECURITY_AUDIT_2026-05-25.md — STATUS write still blocked (permission not granted).
secp256k1 CONFIRMED-SAFE again (16th consecutive confirmation).
