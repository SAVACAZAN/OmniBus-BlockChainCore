---
name: Fifth audit 2026-05-11
description: Pass 5 findings — 2 NEW HIGH (guardian removeGuardian unauth, submitSlashEvidence placeholder), 4 NEW MEDIUM/LOW; 1 CRITICAL + 8 HIGH still open; secp256k1 CONFIRMED-SAFE again
type: project
---

# Fifth Audit 2026-05-11 — Summary

## New Findings
- N5-01 HIGH: rpc_server.zig:7235-7248 — submitSlashEvidence uses hardcoded placeholder block hashes/sigs (0xAA, 0xBB, 0x11, 0x22); staking.verifyDoubleSign trivially satisfied; executeSlash removes 33% stake + 10% to attacker-specified reporter. Fix: parse real hex params, verify secp256k1 sigs, remove placeholder constants.
- N5-02 HIGH: guardian.zig:100-110 — removeGuardian accepts only account address, immediately sets r.status=.removed with no guardian_sig, no owner_sig verification. Activation has 20k-block delay; removal is instant and unauthenticated. Fix: add both sig params, verify via Secp256k1Crypto.verify, add 20k-block cooldown.
- N5-03 MEDIUM: state_trie.zig:93-104 — calculateRootHash iterates AutoHashMap (non-deterministic order); two nodes with same accounts produce different root hashes. Fix: sort by account address before hashing.
- N5-04 MEDIUM: rpc_server.zig:7521-7544 — two JSON string extractors (extractArrayStr vs extractStringFromArrayParams) with inconsistent escape handling, different handlers use different functions. Fix: unify on one RFC 8259 compliant function.
- N5-05 LOW: bootstrap.zig:44-51 — SEED_PEERS all 127.0.0.1/RFC-1918; mainnet VPS mines in isolation. Fix: add public seed IPs.
- N5-06 LOW: binary_codec.zig:30-44 — Varint.decodeU64 no max-byte guard; shift > 63 in ReleaseFast = UB. Fix: if (bytes_read >= 9) return error.VarintTooLong.

## Confirmed Safe
- secp256k1.zig: CONFIRMED SAFE (3rd confirmation). Constant-time scalar multiply, no std.mem.eql on secrets, correct low-S normalization.
- p2p.zig eclipse limits: CONFIRMED SAFE. SEV-02 fix still in place.
- guardian.zig verifyGuardedTx: SAFE. Only removeGuardian broken.
- mempool.zig add() path: SAFE (partial). replaceByNonce() still open.

## All Open Issues: 1 CRITICAL, 10 HIGH, 11 MEDIUM, 3 LOW = 25 total

## Prior Issues Status
All 18 prior-open issues remain OPEN (no fixes merged since 2026-05-10 audit).
SEV-02 remains FIXED.
