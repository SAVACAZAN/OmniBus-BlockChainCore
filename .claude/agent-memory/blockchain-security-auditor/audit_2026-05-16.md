---
name: audit-2026-05-16
description: Eleventh security audit findings — 2 NEW (SEV-58 gov tier self-decl, SEV-59 extractStr no escapes), SEV-52 FIXED, SEV-26/44 partial fix, SEV-46 mitigated; 16 total open
metadata:
  type: project
---

Audit 11 (2026-05-16). Files audited: secp256k1.zig, p2p.zig, mempool.zig, rpc_server.zig, transaction.zig, state_trie.zig, governance_onchain.zig, grid_engine.zig, finality.zig, bls_signatures.zig, price_oracle.zig.

**Why:** Continued pre-mainnet security hardening series.

**How to apply:** Add to cumulative open issues table in MEMORY.md.

## NEW findings

SEV-58 (HIGH) — rpc_server.zig:16054 handleGovVote reads `tier` from request body without verification. `tierWeight("ZEN")=1000` vs default 10 — 100x amplification. Plus, signature fields are read but never verified before vote is committed. Compound with SEV-30.

SEV-59 (MEDIUM) — rpc_server.zig:7771 extractStr inner loop does not handle `\"` escape sequences. Values truncated at backslash before closing quote. Can cause hexToBytes to fail silently, potentially bypassing sig verification in handlers using `catch continue`.

## Status changes

SEV-52 (grid_cancel no auth): FIXED — grid_engine.zig:172 checks owner; rpc_server.zig:14578 passes owner from body.

SEV-26/44 (oracle_recordHeader no sig): PARTIALLY FIXED — cross-chain oracle path now verifies quorum sigs. Price_oracle.zig submitPrice path (SEV-45) still open.

SEV-46 (grid order_id collision): MITIGATED — MAX_LEVELS=100, stride=10000 prevents collision.

SEV-03 (mempool replaceByNonce no sig): HIGH-05 fix is in mempool.zig code — verifier runs before RBF branch if non-null. Status depends on main.zig wiring.

## Open count: 16 (2 CRITICAL, 10 HIGH, 4 MEDIUM)

Top 2 blockers: SEV-50 (state_trie non-deterministic root), SEV-33/42 (PQ-OMNI always returns true).

STATUS folder write was blocked again this pass.

[[audit_2026-05-15]]
