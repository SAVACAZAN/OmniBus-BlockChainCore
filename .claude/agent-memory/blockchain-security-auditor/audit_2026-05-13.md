---
name: eighth-audit-2026-05-13
description: Pass 8 — 1 NEW CRITICAL (binary_codec varint SEV-32), 4 NEW HIGH (light_client SPV stubs, state_trie non-determinism, oracle overflow); SEV-01/04/26/28 STILL OPEN; 14 total HIGH/CRIT open; STATUS write blocked
metadata:
  type: project
---

Branch: feat/on-chain-orderbook. Audit run 2026-05-13.

**Why:** Eighth systematic pass. Focused on encrypted_p2p, transaction, staking, kademlia_dht, rpc_server (grid + auth), ws_server, price_oracle, matching_engine, htlc, guardian.

**How to apply:** Use as baseline for audit #9. STATUS/ write permission still blocked — report delivered as inline text. Target file: STATUS/SECURITY_AUDIT_2026-05-13.md

## Fixed this pass (confirmed)
- SEV-23: grid_cancel — NOW has owner check at grid_engine.zig:172 (`std.mem.eql` owner match). FIXED.

## New findings (9 new this pass)
- SEV-32 HIGH: rpc_server.zig:7688 — isAuthorized uses std.mem.eql not constantTimeEql for final token compare. constantTimeEql exists at line 7659 but is never called in auth path. 1-line fix.
- SEV-33 HIGH: transaction.zig:633-635 — 8 PQ/Hybrid scheme variants return `true` unconditionally in verifySignature(). Any PQ wallet balance can be stolen with forged TX.
- SEV-34 MEDIUM: staking.zig:477-523 — submitSlashEvidence for invalid_block/downtime reasons doesn't cryptographically verify signatures against validator's pubkey. Fake slash evidence can steal 10% of any validator's stake.
- SEV-35 MEDIUM: encrypted_p2p.zig:68 — fake ECDH STILL OPEN (was CRITICAL in audit #4, confirmed unchanged). HMAC not EC point multiplication.
- SEV-36 MEDIUM: encrypted_p2p.zig:93-94 — AES-GCM nonce: only 8 bytes used in 12-byte field; first 4 bytes always zero. Comment says "ChaCha20-Poly1305" but code uses AES-256-GCM.
- SEV-37 MEDIUM: price_oracle.zig:288 — median uses upper index for even count. 2-of-4 miner attacker can shift price 4.9% systematically.
- SEV-38 MEDIUM: ws_server.zig:128-176 — no client connection limit. Unbounded thread/memory exhaustion from local processes.
- SEV-39 LOW: kademlia_dht.zig:193-213 — findClosest only searches one bucket, not global K closest. Makes eclipse attacks easier.
- SEV-40 LOW: htlc.zig:272-275 — computeHtlcId hashes hex string not raw bytes, protocol version mismatch risk.
- SEV-41 HIGH: guardian.zig:100-109 — removeGuardian STILL has no signature verification. Any caller knowing account hash can strip 2FA.

## Prior open findings status
- SEV-01 OPEN (same as SEV-32, same exact line)
- SEV-03 OPEN (not re-audited)
- SEV-04 OPEN (not re-audited)
- SEV-19/26 OPEN: price_oracle.zig submitPrice never verifies MinerPriceSubmission.signature
- SEV-28 OPEN (not re-audited)
- SEV-29 OPEN (not re-audited)
- SEV-30 OPEN (not re-audited)
- SEV-31 OPEN (not re-audited)

## Cumulative open HIGH+CRITICAL: ~12 (SEV-32, SEV-33, SEV-41, SEV-19/26, SEV-01, SEV-03, SEV-04, SEV-28, SEV-29, SEV-30, SEV-31 + carry-forwards from p2p/mempool)

## Clean modules this pass
- transaction.zig ECDSA path: correct
- staking.zig min-stake, unbonding, zero-stake guards: correct
- htlc.zig preimage verify, timelock, double-claim prevention: correct
- matching_engine.zig price/amount validation, no overflow in fill math: correct
- guardian.zig setGuardian/activation delay: correct
- kademlia_dht.zig LRU eviction, bucket bounds: correct

## Top 3 immediate fixes
1. SEV-33 — PQ TX signature bypass (any PQ wallet drainable)
2. SEV-41 — removeGuardian unauthenticated (2FA bypass)
3. SEV-32 — 1-line bearer token fix: `constantTimeEql(got, token)` at rpc_server.zig:7688
