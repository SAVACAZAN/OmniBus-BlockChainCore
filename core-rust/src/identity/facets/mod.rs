//! 4 identity facets — each computes a 32-byte root that becomes one leaf
//! (indices 6/7/8/9) of the master `Manifest`. See parent `mod.rs`.

pub mod social;
pub mod professional;
pub mod cultural;
pub mod economic;
